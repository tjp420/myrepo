## Unreleased

- 2025-12-13 — chore: Add pinned minimal dev/test requirements and CI improvements; added `pyproject.toml` so `-e .` installs in CI; fixed test-collection and exported `get_enhanced_framework` stub so tests can monkeypatch it; CI: green.

- PR: create changelog PR to review this entry.
